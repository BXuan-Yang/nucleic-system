<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper  namespace="com.ydf.user.mapper.SysMenuMapper">
    <select id="selectPermsByUserId" resultType="java.lang.String">
        SELECT
            DISTINCT m.`perms`
        FROM
            user_role AS ur
        LEFT JOIN
            sys_role AS r
        ON
            ur.`role_id` = r.`id`
        LEFT JOIN
            role_menu AS rm
        ON
            ur.`role_id` = rm.`role_id`
        LEFT JOIN
            `sys_menu` m
        ON
            m.`id` = rm.`menu_id`
        WHERE
            ur.`user_id` = #{userId}
        AND
            r.`is_deleted` = 0
        AND
            m.`is_deleted` = 0
    </select>
</mapper>